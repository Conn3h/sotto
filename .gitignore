.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
.DS_Store
# Built bundles at the repo root only (anchored on purpose).
/*.app/
# Regenerated by `make icon`; only the .icns is tracked.
Resources/AppIcon.iconset/
